import create from 'zustand'

export interface {{pascalCase featureName}}Store {
    // Store properties
}

export const use{{pascalCase featureName}}Store = create<{{pascalCase featureName}}Store>((set, get) => ({
    // Store implementations
}))
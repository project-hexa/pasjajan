import { ReactNode } from "react";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "{{titleCase featureName}}",
};

export default function {{pascalCase featureName}}Layout({children}: {children: ReactNode}) {
  return (
    <main>
        {children}
    </main>
  )
}
